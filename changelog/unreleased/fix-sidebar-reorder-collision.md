---
type: fixed
---
Shift+↑/↓ on a repo header no longer silently no-ops when a neighbouring repo's stored sort key happens to collide with the move's seeded fallback value. Repo positions are now renumbered with unique sequential keys after every swap.
