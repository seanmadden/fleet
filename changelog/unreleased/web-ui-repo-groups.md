---
type: improved
---
Web UI now groups sessions by repo with sticky section headers (repo name + session count), mirroring the TUI sidebar's grouping. Worktree-backed sessions sit under their main repo's header rather than appearing as separate groups, so a phone-sized session list stays scannable when several repos are active at once.
