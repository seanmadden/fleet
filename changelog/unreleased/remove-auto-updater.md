---
type: removed
---
Auto-updater removed. `fleet update`, the on-launch update check, the `auto_update` config field, and the `FLEET_AUTO_UPDATE_DISABLED` env var are gone. Install via Homebrew, `install.sh`, `go install`, or `install-dev.sh` and manage upgrades yourself.
